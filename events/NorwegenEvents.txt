namespace = phm_norwegen_events

country_event = {
    id = phm_norwegen_events.1
    title = phm_norwegen_events.1.t
    desc = phm_norwegen_events.1.d
    picture = WOUNDED_SOLDIERS_eventPicture

    major = yes
    fire_only_once = yes
    trigger = {
        tag = NOR
        has_global_flag = phm_konstantinopel_flag
    }

    mean_time_to_happen = {
        days = 30
    }

    option = {
        name = phm_norwegen_events.1.a
        TUR = {
            add_opinion = {
                who = ROOT
                modifier = phm_friendly_opinion_decay_50_5
            }
        }
        add_opinion = {
            who = TUR
            modifier = phm_friendly_opinion_decay_50_5
        }

    }

    option = {
        name = phm_norwegen_events.1.b
        add_prestige = 50
        hidden_effect = {
            set_country_flag = phm_anti_osman
            TUR = {
                country_event = {
                    id = phm_osman_events.2
                }
            }
        }
    }
}

country_event = {
    id = phm_norwegen_events.2
    title = phm_norwegen_events.2.t
    desc = phm_norwegen_events.2.d
    picture = FROZEN_VILLAGE_eventPicture

    major = yes
    fire_only_once = yes

    trigger = {
        tag = NOR
        if = {
            limit = {
                OR = {
                    1104 = { is_empty = yes }
                    1105 = { is_empty = yes }
                }
            }
            OR = {
                1104 = {
                    OR = {
                        num_of_units_in_province = {
                            who = ROOT
                            type = infantry
                            amount = 3
                        }
                        country_or_non_sovereign_subject_holds = ROOT
                    }
                }
                1105 = {
                    OR = {
                        num_of_units_in_province = {
                            who = ROOT
                            type = infantry
                            amount = 3
                        }
                        country_or_non_sovereign_subject_holds = ROOT
                    }
                }
            }
        }
        else = {
            owns_or_non_sovereign_subject_of = 1104
            owns_or_non_sovereign_subject_of = 1105
        }
    }

    option = {
        name = phm_norwegen_events.2.a
        country_event = {
            id = flavor_nor.34
        }
    }
}

country_event = {
    id = phm_norwegen_events.3
    title = phm_norwegen_events.3.t
    desc = phm_norwegen_events.3.d
    picture = COMET_SIGHTED_eventPicture

    major = yes
    fire_only_once = yes

    trigger = {
        tag = SCA
        is_year = 1531
    }

    option = {
        name = phm_norwegen_events.3.a
        add_country_modifier = {
            name = "phm_nord_diszi"
            duration = -1
        }
    }
}